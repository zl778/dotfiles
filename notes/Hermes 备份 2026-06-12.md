---
created: 2026-06-12T11:56
tags: [hermes, backup, system-maintenance]
---

# Hermes 全量备份 — 2026-06-12

## 备份文件

```
~/Desktop/hermes-full-backup-20260612.tar.gz
```

大小：**33MB**

## 备份内容

| 内容         | 文件                 | 说明                       |
| ---------- | ------------------ | ------------------------ |
| 配置         | `config.yaml`      | Hermes 主配置               |
| API 密钥     | `.env`             | 各 provider 的 API key（敏感） |
| OAuth 令牌   | `auth.json`        | Codex 等 OAuth 凭证（敏感）     |
| 会话/记忆/定时任务 | `state.db`         | **最重要的数据**（29MB）         |
| 自定义 Skills | `skills/`          | 已安装的 skill（22MB）         |
| 插件         | `plugins/`         | 自定义插件                    |
| 个人 Profile | `profiles/wukong/` | 另一个 profile（32MB）        |
| 配对授权       | `pairing/`         | 平台授权记录                   |
| 记忆         | `memories/`        | 跨会话持久记忆                  |

## 排除的内容

以下内容可重新安装，未纳入备份（共约 3.3GB）：

- `~/.hermes/hermes-agent/`（2.9G）— 源码 + venv，可 `hermes update` 重建
- `~/.hermes/node/`（388M）— Node.js 运行时，可自动安装
- `~/.hermes/bin/`（56M）— 内置工具二进制
- `~/.hermes/lsp/`（33M）— LSP 语言服务
- `~/.hermes/logs/`（10M）— 运行日志
- `~/.hermes/cache/`（1.5M）— 缓存

## 还原方法

关闭 Hermes（CLI 和桌面应用）后执行：

```bash
tar xzf ~/Desktop/hermes-full-backup-20260612.tar.gz -C ~/
```

## 备份时环境状态

- Hermes Desktop 已更新到 v0.16+
- 显示语言已设为 `zh`（简体中文）
- Provider: deepseek (deepseek-v4-flash)
- 已安装 skill 数量：含自定义 skill 16 个
- Profiles：default + wukong
- 上次 `hermes update`：2026-06-12 11:43
- Pre-update 快照：`20260612-034312-pre-update`
